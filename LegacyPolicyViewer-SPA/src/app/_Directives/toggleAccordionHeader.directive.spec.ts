/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ToggleAccordionHeaderDirective } from './toggleAccordionHeader.directive';

describe('Directive: ToggleAccordionHeader', () => {
  it('should create an instance', () => {
    const directive = new ToggleAccordionHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
